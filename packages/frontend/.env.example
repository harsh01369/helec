# Frontend Environment Variables
# Copy this to .env and update with your values

# Backend API URL (include protocol, no trailing slash)
# Note: In SvelteKit/Vite, environment variables prefixed with PUBLIC_
# are exposed to the client-side code
PUBLIC_BACKEND_URL=http://localhost:3000

# For production (Vercel), set this environment variable in Vercel dashboard:
# PUBLIC_BACKEND_URL=https://helec-backend.onrender.com
